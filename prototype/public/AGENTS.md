<!-- Parent: ../AGENTS.md -->

# prototype/public

## Purpose

Vite의 정적 자산 루트. 직접 파일은 없고 화면이 fetch로 읽는 데이터 디렉터리만 담는다.

## Subdirectories

| Directory | Purpose |
|-----------|---------|
| `data/` | `scripts/build_web_data.py`가 생성하는 화면용 정적 희소 집계 JSON (see `data/AGENTS.md`) |
